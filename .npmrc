# The toolchain has a peer-dependency conflict (@swc/core vs @swc/helpers), so
# installs must use legacy peer resolution. Setting it here means Vercel (and
# any fresh `npm install`) picks it up automatically — no manual flag needed.
legacy-peer-deps=true
